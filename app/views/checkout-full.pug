extends components/layout.pug

block titlepage
    title="Demo Shop - Checkout full page"

block content
    div#checkout-full
        div#iframe
block scriptsBody
    script.
        window.onload = function(){
            const urlLocal = "!{url_token}";
            const paymentID = "!{payment_id}";
            window.openCheckout(urlLocal, paymentID);
        }