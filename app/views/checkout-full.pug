extends components/layout.pug

block content
    div#checkout-full
        div#iframe
block scriptsBody
    script.
        window.onload = function(){
            const urlLocal = "http://localhost:3030/" + "!{url_token}".replace("https://sandbox.bexs.com.br/", "")
            window.openCheckout(urlLocal);
        }