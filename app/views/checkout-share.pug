extends components/layout.pug

block content
    div#checkout-share
        div.container-fluid
            .row.top-xs
                .col-sm-9.col-xs-12
                    div#iframe
                .col-sm-3.col-xs-12.first-xs.last-sm
                    .card-content
                        h3.fake-heading
                        .row-divisor
                        .fake-description
                            .fake-label
                            .fake-price
                        .fake-description
                            .fake-label
                            .fake-price
                        .fake-description
                            .fake-label
                            .fake-price
                        .row-divisor
                        .fake-description.total-price
                            .fake-label
                            .fake-price
block scriptsBody
    script.
        window.onload = function(){
            const urlLocal = "http://localhost:3030/" + "!{url_token}".replace("https://sandbox.bexs.com.br/", "")
            const paymentID = "!{payment_id}";
            window.openCheckout(urlLocal, paymentID);
        }